<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:layout_marginTop="5dp"
    android:padding="12dp"
    android:background="@drawable/archivo2"
    android:gravity="center_vertical"
    android:backgroundTint="@color/Color4">

    <LinearLayout
        android:id="@+id/iv_abrir"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/iv_item_personaje"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:scaleType="centerCrop"
            android:background="@color/Color3"
            android:contentDescription="Foto de personaje" />

        <TextView
            android:id="@+id/tv_item_nombre"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/Color1" />
    </LinearLayout>

    <ImageView
        android:id="@+id/iv_borrar"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@android:drawable/ic_menu_delete"
        android:contentDescription="Presiona para borrar"
        app:tint="@color/Color1" />
</LinearLayout>